# ==============================================================================
# ACCOUNTS CONFIGURATION - ENVIRONMENT VARIABLES
# ==============================================================================
# Purpose: This file shows the environment variables used in accounts.yaml
# Usage: Copy to .env and fill in actual credentials
# Security: Keep .env file secure - never commit to version control!
# ==============================================================================

# ==============================================================================
# TopstepX API CREDENTIALS (Required)
# ==============================================================================
# These credentials are used to authenticate with the TopstepX API
# Get these from: TopstepX Dashboard → Account Settings → API Keys

# Your TopstepX username/email
PROJECT_X_USERNAME=your_username_or_email@example.com

# Your TopstepX API key (keep this secret!)
PROJECT_X_API_KEY=your_api_key_here_from_topstepx_dashboard

# ==============================================================================
# USAGE IN accounts.yaml
# ==============================================================================
# The variables above are referenced in accounts.yaml like this:
#
#   topstepx:
#     username: "${PROJECT_X_USERNAME}"    # Will be replaced from .env
#     api_key: "${PROJECT_X_API_KEY}"      # Will be replaced from .env
#
# This keeps credentials out of the YAML file for better security.
# ==============================================================================

# ==============================================================================
# OPTIONAL: Additional Credentials
# ==============================================================================
# These are loaded from the root .env file and used globally

# Claude AI API (for AI features - optional)
# ANTHROPIC_API_KEY=your_anthropic_key_here

# Discord Notifications (optional)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Telegram Notifications (optional)
# TELEGRAM_BOT_TOKEN=your_bot_token
# TELEGRAM_CHAT_ID=your_chat_id

# ==============================================================================
# NOTES
# ==============================================================================
# 1. Copy this file to .env (same directory as accounts.yaml)
# 2. Fill in your actual credentials
# 3. The .env file is in .gitignore (never committed to git)
# 4. Environment variables take precedence over hardcoded values
# 5. ${VAR} syntax in accounts.yaml will be replaced with .env values
# ==============================================================================
